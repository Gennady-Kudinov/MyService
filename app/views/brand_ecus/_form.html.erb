<%= form_with(model: brand_ecu) do |form| %>
  <% if brand_ecu.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brand_ecu.errors.count, "error") %> prohibited this brand_ecu from being saved:</h2>

      <ul>
        <% brand_ecu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
