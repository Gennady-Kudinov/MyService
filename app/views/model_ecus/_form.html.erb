<%= form_with(model: model_ecu) do |form| %>
  <% if model_ecu.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(model_ecu.errors.count, "error") %> prohibited this model_ecu from being saved:</h2>

      <ul>
        <% model_ecu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

   <div>
    <%= form.label :brand_ecu_id, style: "display: block" %>
    <%= form.text_field :brand_ecu_id, value: params[:brand_ecu_id] %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
