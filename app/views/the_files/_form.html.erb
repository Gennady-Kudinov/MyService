<%= form_with(model: the_file) do |form| %>
  <% if the_file.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(the_file.errors.count, "error") %> prohibited this the_file from being saved:</h2>

      <ul>
        <% the_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :file, style: "display: block" %>
    <%= form.file_field :file %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_area :title %>
  </div>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
